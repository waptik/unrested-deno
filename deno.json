{
  "tasks": {
    "check": "deno check mod.ts *.ts",
    "test": "deno test --allow-write --allow-read --allow-net --trace-ops",
    "bench": "deno bench",
    "prep": "deno task check && deno fmt && deno lint && deno publish --dry-run && deno task test"
  },
  "lock": false
}